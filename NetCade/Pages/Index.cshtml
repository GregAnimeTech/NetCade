@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<html>
<head>
    <style>
        .button {
            background-color: red; /*red*/
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            margin: 4px 2px;
            cursor: pointer;
            font-size: 14px;
            border-radius: 12px;
        }

        .button2 {
            background-color: blue; /*blue*/
        }

        .button3 {
            background-color: black; /*black*/
        }
    </style>
</head>

<body>
    <div class="text-center">
        <h1 class="display-4">Welcome to NetCade</h1>
        @*<h2>It's @Model.Time  right now on the server!</h2>*@
        <p>
            You get to chose which game you want to play.
        </p>
        <a href="https://localhost:44380/Game1" target="_self"><button class="button">Game 1</button></a>
        <a href="https://localhost:44380/Game2" target="_self"><button class="button button2">Game 2</button></a>
        <a href="https://localhost:44380/StoryGame" target="_self"><button class="button button3">Story Game</button></a>
    </div>

    @*This is a prototype of database for games*@
    @*<div id="featuredProduct" class="row">
        <div class="col-sm-8">
            <img alt="Featured Product" src="~/Images/Products/@Model.FeaturedUserName.UserName" class="img-fluid rounded" />
        </div>
        <div id="featuredProductInfo" class="col-sm-4">
            <div id="productInfo">
                <h2>@Model.FeaturedUserName.UserName</h2>
                <p class="name">$@string.Format("{0:f}", Model.FeaturedUserName.Name)</p>
                <p class="password">@Model.FeaturedUserName.Password</p>
            </div>
            <div id="callToAction">
                <a class="btn btn-danger order-button" asp-page="/order" asp-route-id="@Model.FeaturedUserName.UserName" title="Order @Model.FeaturedUserName.Name">Order Now</a>
            </div>
        </div>
    </div>
    <div id="productsWrapper" class="row">
        @foreach (var account in Model.UserAccount)
        {
            <div class="col-sm-3">
                <a asp-page="/order" asp-route-id="@account.UserName" title="Order @account.Name">
                    <div class="productInfo">
                        <h3>@account.Name</h3>
                        <img class="product-image img-fluid img-thumbnail" src="~/Images/Products/Thumbnails/@account.UserName" alt="Image of @account.Name" />
                        <p class="password">@account.Password</p>
                    </div>
                </a>
                <div class="action">
                    <p class="price float-left">$@string.Format("{0:f}", account.Password)</p>
                    <a class="btn btn-sm btn-danger order-button float-right" asp-page="/order" asp-route-id="@account.UserName" title="Order @account.Name">Order Now</a>
                </div>
            </div>*@
        }
    </div>

</body>
</html>